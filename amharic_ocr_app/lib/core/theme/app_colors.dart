import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color seed = Color(0xFF5B3DF6);
  static const Color accent = Color(0xFF19C6B0); // teal accent for CTAs/success
  static const Color warning = Color(0xFFF5A524);
  static const Color danger = Color(0xFFE5484D);
}
